<li class="burger-list">
    {{burger_name}}
        <button class="devour-btn" data-id="{{id}}" data-newsleep="{{devoured}}"> 
            {{#if devoured}}DEVOUR! {{else}}EATEN{{/if}}
            </button>
</li>